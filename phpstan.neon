parameters:
  level: max
  paths:
    - src
  ignoreErrors:
    - '#.* of class [a-zA-Z0-9\\_]+ constructor expects [a-z]+\|?[a-z]*, mixed given.#'
    - '#Argument of an invalid type mixed supplied for foreach, only iterables are supported.#'

